/**
 * Built-in extensions bundled with @earendil-works/pi-coding-agent.
 *
 * These are injected by DefaultResourceLoader when the corresponding
 * settings flag is enabled (default: enabled).
 */

export { default as historyRetrievalExtension } from "./history-retrieval/index.ts";
